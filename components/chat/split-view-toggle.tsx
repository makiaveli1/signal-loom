'use client';

export function SplitViewToggle() {
  // Intentionally empty — preset switching is now handled by the PanePresetSwitcher
  // in the single-pane title bar. This component is kept as a placeholder hook
  // for any future inline split controls.
  return null;
}
